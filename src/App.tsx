import React from 'react';
import { Box } from '@mui/material';
import SideBar from "./components/SideBar/SideBar";

const App = () => {
    return (
        <Box sx={{ padding: 4 }}>
            <SideBar />
        </Box>
    );
};

export default App;
